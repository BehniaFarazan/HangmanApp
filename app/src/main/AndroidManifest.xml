<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.behnia.s165203superhangman">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HelpActivity" />
        <activity android:name=".WinActivity" />
        <activity android:name=".PlaySingleModeActivity" />
        <activity android:name=".LostActivity" />
        <activity android:name=".HighsScoreActivity" />
        <activity android:name=".MainMenu" />
        <activity android:name=".PlayerMode" />
        <activity android:name=".ChooseWordActivity" />
        <activity android:name=".PlayMultiModeActivity"></activity>
    </application>

</manifest>